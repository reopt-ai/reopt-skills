# reopt-eav

EAV schema management guidance for Brandapp projects.

## Use When

- local schema changes must be reviewed before sync
- CI needs a safe EAV workflow
- server state needs to be pulled back into `eav.schema.ts`

